@model APIApplication.Models.Archive

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Archive</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ArchiveId" />
            <div class="form-group">
                <label asp-for="ArchiveUserId" class="control-label"></label>
                <!-- Use ViewData instead of ViewBag -->
                @Html.DropDownListFor(model => model.ArchiveUserId, ViewData["UsersSelectList"] as SelectList, "Select User", new { @class = "form-control" })
                <span asp-validation-for="ArchiveUserId" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="ArchiveDate" class="control-label"></label>
                <input asp-for="ArchiveDate" class="form-control" />
                <span asp-validation-for="ArchiveDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a class="btn btn-secondary" asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
